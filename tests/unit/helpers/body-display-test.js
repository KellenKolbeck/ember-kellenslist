import { bodyDisplay } from '../../../helpers/body-display';
import { module, test } from 'qunit';

module('Unit | Helper | body display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bodyDisplay(42);
  assert.ok(result);
});
